* Server

```
$ cd server
$ npm run install
$ cd httpd
$ vim config/default.json
> Replace "setgid", "setuid", "password" and etc.
$ sudo npm run start
```

* Player

```
$ cd player
$ pod install
```
