* Server

```
$ cd server
$ npm run install
$ cd content-server
$ vim config/default.json
> Replace "setgid", "setuid", "password" and etc.
$ sudo npm run start
```

* Client

```
$ cd client
$ pod install
```
